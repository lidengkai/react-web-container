export type Store = {
  /** 加载中 */
  loading: boolean
}

export type Props = {
}

export declare namespace ContainerUserInfo {
  type Props = {
  }
  type FormType = {
    username: string
    password: string
  }
}
